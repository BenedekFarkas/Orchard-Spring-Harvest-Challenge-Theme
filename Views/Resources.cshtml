@{
    // Inclusions and Requires work as normally (when testing locally and also when deployed to DotNest) and they keep their order.
    // Be aware that you can't use any other chained method then demonstrated here.
    Style.Include("site.css");
}